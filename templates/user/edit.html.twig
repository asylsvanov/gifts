{% extends 'base.html.twig' %}
{% block pagetitle %}{% trans %}Edit profile{% endtrans %}{% endblock pagetitle %}

{% block body %}
<div class="col-md-6">
	{{ form_start(form) }}
	{{ form_widget(form) }}


	<div class="col-md-6 col-xxl-5 form-group mt-3" style="">
		<div class="form-widget">
			<button type="submit" class="btn btn-primary">
				<i class="fa fa-save" aria-hidden="true"></i>
				{{ 'action.save'|trans }}
			</button>
		</div>
	</div>

	{{ form_end(form) }}

</div>
{% endblock %}
